<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Медиафайлы</title>
    <style>
        /* Стили для полноэкранного отображения изображения */
        body, html {
            margin: 0;
            padding: 0;
            height: 100%;
            overflow: hidden;
            background-color: black;
        }
        img {
            width: 100%;
            height: 100%;
            object-fit: contain; /* Сохраняет пропорции изображения */
        }
        #playButton {
            position: absolute;
            bottom: 20px;
            left: 50%;
            transform: translateX(-50%);
            padding: 10px 20px;
            font-size: 16px;
            background-color: #4CAF50;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <!-- Изображение -->
    <img src="https://i.postimg.cc/Prn6QYVh/3fa5157f40bcbaeb596cb62534e5dcb2-1-1.png" alt="Картинка">

    <!-- Кнопка для воспроизведения аудио -->
    <button id="playButton">Play Audio</button>

    <!-- Аудио -->
    <audio id="audio" controls style="display:none;">
        <source src="https://voca.ro/18Y7EOWMBa79" type="audio/mpeg">
        Ваш браузер не поддерживает аудио элемент.
    </audio>

    <script>
        // Автоматическое воспроизведение аудио
        document.addEventListener('DOMContentLoaded', function() {
            var audio = document.getElementById('audio');
            var playButton = document.getElementById('playButton');

            // Устанавливаем громкость на максимум
            audio.volume = 1.0;

            // Пытаемся воспроизвести аудио автоматически
            audio.play().then(() => {
                // Если аудио воспроизводится, скрываем кнопку
                playButton.style.display = 'none';
            }).catch(() => {
                // Если автоматическое воспроизведение заблокировано, показываем кнопку
                playButton.style.display = 'block';
            });

            // Воспроизводим аудио при нажатии на кнопку
            playButton.addEventListener('click', function() {
                audio.play();
                playButton.style.display = 'none'; // Скрываем кнопку после начала воспроизведения
            });
        });
    </script>
</body>
</html>
